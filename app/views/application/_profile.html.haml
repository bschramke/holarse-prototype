#profile
  %ul.profile
    %li
      - if current_user
        = link_to "Hallo, #{current_user.username}", current_user
        = link_to "Abmelden", logout_path
      - else
        = link_to "Login", login_path

= semantic_form_for [commentable, Comment.new] do |f|
  .form-group
    = f.input :content, as: :text, input_html: { class: "form-control", rows: 3 }
  = f.actions do 
    .form-group
      = f.action :submit, as: :button, button_html: { class: "btn btn-default btn-primary" }


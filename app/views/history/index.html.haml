%ul
  - @revisions.each do |revision|
    %li
      = revision.created_at
      = revision.user.username
      = revision.changelog
      = link_to t('.show'), url_for(:controller => :history, :action => :show, "#{@parent.class.name.underscore}_id".to_sym => @parent.id, :id => revision)

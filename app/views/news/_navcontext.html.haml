%li
  = link_to "Neue News", new_news_path
- if @news.present? && !@news.new_record? && current_page?(news_path(@news))
  %li
    = link_to "Bearbeiten", edit_news_path(@news)
  %li
    = link_to news_history_index_path(@news) do
      Revisionen
      = content_tag :span, :class => "badge" do
        #{@news.revisions.length}

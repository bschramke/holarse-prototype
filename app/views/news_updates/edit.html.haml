= semantic_form_for [@news, @news_update] do |f|
  = f.inputs do
    .form-group
      = f.input :content, as: :text, input_html: { class: "form-control" }
  = f.action :submit, as: :button, button_html: { class: "btn btn-primary" }

%h1 Articles#new
%p Find me in app/views/articles/new.html.haml

.edit_content
  = semantic_form_for @article do |f|
    = f.semantic_errors :title, :alternate_title, :content
    = f.inputs :title, :alternate_title, :content
    = f.actions

